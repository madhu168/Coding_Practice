Python code cheat sheet


1) for finding size of lists or set - len()
2) defining set or list in python
    a = list()
    b = set()
3) python list has append - a.append("1")
4) python set has add - b.add("1")
5) python operators
      1) % - gets reminder 
      2) // - gets integer quotient

6) There is for else in python, In Python, the else clause in a for loop is executed if the loop iterates over all elements without encountering a break statement. This is a unique feature of Python's for...else construct.